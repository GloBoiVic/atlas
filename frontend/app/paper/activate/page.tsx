import { AppShell } from '../../../components/app-shell';
import { PaperActivation } from '../../../components/paper-activation';

export default function PaperActivationPage() {
  return (
    <AppShell>
      <PaperActivation />
    </AppShell>
  );
}
