# T008 — PostgreSQL migration and completed-fixture remediation

Status: `BUILD COMPLETE`

Repair `0014_result_metric_state_details` to drop the actual constraint names
created under the repository naming convention in both directions, preserving a
complete upgrade → downgrade → upgrade cycle. Update only stale integration
fixtures/tests that violate `COMPLETED => persisted ExperimentResult exists`;
do not weaken application behavior. Add focused regression coverage as needed.

## Changes

- Updated `backend/persistence/migrations/versions/0014_result_metric_state_details.py`
  to drop the physical PostgreSQL names generated by the repository check
  constraint naming convention, while retaining logical names for creation in
  both upgrade and downgrade.
- Updated the dependent migration downgrades in
  `backend/persistence/migrations/versions/{0007_phase_5_metric_contract,0008_proposal_constraints,0010_experiment_gap_decisions,0013_result_quality_degraded}.py`
  to use converted physical names, completing the downgrade path to the
  established `0006_phase_4_persistence` schema.
- Updated `backend/tests/integration/test_migrations.py` to assert both physical
  metric-state constraint names are present after upgrade.
- Updated `GatedRunner` in
  `backend/tests/integration/test_experiment_lifecycle.py` to persist a minimal
  valid `ExperimentResult` through `ExperimentRepository.create_result` before
  completing an Experiment.
- Updated the direct completed fixture in
  `backend/tests/integration/test_api_experiments.py` to persist its result and
  metric states before setting terminal status.

## Checks

- `python -m compileall -q backend && git diff --check` — **PASS**.
- `pytest -q backend/tests/test_migration_revision.py backend/tests/experiments/test_result_state.py` — **3 passed**.
- `ATLAS_TEST_DATABASE_URL=postgresql+psycopg://atlas:atlas@127.0.0.1:5432/atlas_test pytest -q backend/tests/integration/test_migrations.py` — **2 passed** (includes upgrade → downgrade → upgrade and base cycle).
- `ATLAS_TEST_DATABASE_URL=postgresql+psycopg://atlas:atlas@127.0.0.1:5432/atlas_test pytest -q backend/tests/integration/test_experiment_lifecycle.py` — **5 passed**.
- `ATLAS_TEST_DATABASE_URL=postgresql+psycopg://atlas:atlas@127.0.0.1:5432/atlas_test pytest -q backend/tests/integration/test_api_experiments.py` — **9 passed**, 4 pre-existing warnings.

## Concerns

- Running migration and lifecycle/API integration files concurrently can
  deadlock because they intentionally reset the shared PostgreSQL test schema;
  the recorded commands were run sequentially.
